---
phase: 05-cold-restore-position-stream-bridge-fix
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - test/playback_trust_test.dart
  - lib/resources/services/my_audio_handler.dart
autonomous: true
requirements: [RESTORE-01]

must_haves:
  truths:
    # Covers ROADMAP success criteria 1, 2, 4 (position + duration shown; suite green)
    - "After restoreIfNeeded() (playImmediately:false deferred-load path) followed by play(), getPositionStream() emits a PositionData whose position equals the saved position (> Duration.zero) within a bounded window"
    - "After the same restore + play sequence, getPositionStream() emits a PositionData whose duration is greater than Duration.zero within the bounded window"
    - "playback_trust_test.dart stays fully green, including the D-05 no-seek invariant test and the existing restore test"
    # Covers ROADMAP success criterion 3 (seek still works)
    - "After restore + play, calling seek() still moves the thumb with no jump-back and no stuck thumb — getPositionStream() reflects the new position"
  artifacts:
    - path: "test/playback_trust_test.dart"
      provides: "FakePlaybackEngine deferred-load mode + cold-restore position-stream regression test"
      contains: "does NOT emit restored position on positionStream until play()"
    - path: "lib/resources/services/my_audio_handler.dart"
      provides: "Position-stream bridge that re-emits position + duration after the deferred native load resolves"
      contains: "getPositionStream"
  key_links:
    - from: "lib/resources/services/my_audio_handler.dart (play override, ~§848)"
      to: "lib/resources/services/my_audio_handler.dart (getPositionStream, ~§785)"
      via: "play() triggers the forked deferred native load; after it resolves the position-stream bridge must re-emit so ProgressBarWidget's combineLatest3 fires with the restored position + resolved duration"
      pattern: "getPositionStream"
    - from: "lib/screens/audiobook_player/widgets/progress_bar_widget.dart (StreamBuilder)"
      to: "lib/resources/services/my_audio_handler.dart (getPositionStream)"
      via: "StreamBuilder<PositionData> bound to audioHandler.getPositionStream() — the ONLY consumer"
      pattern: "audioHandler\\.getPositionStream"
---

<objective>
Diagnose the deferred-load position-stream gap (confirm which of the two candidate mechanisms from CONTEXT.md D-04a/D-04b is real), fix the position-stream bridge so the progress bar reflects the restored position and duration immediately after cold-restore + play, and add a regression test that stays green alongside all existing playback_trust_test.dart invariants.

Purpose: Audio already resumes at the correct saved position after cold-restore (the v1.0 await-ready gate proved that). Only the UI progress bar is wrong — it shows 0:00 / does not advance because the forked just_audio's deferred-load path does not re-emit the restored position on `positionStream` until the native load resolves, and `getPositionStream()`'s `combineLatest3` never re-fires (or fires with a stale/zero duration). RESTORE-01 is the single requirement this plan delivers.

Output: A passing regression test that reproduces the bug in FakePlaybackEngine, a minimal fix in the position-stream bridge layer (D-08) that does NOT add a seek in the `playImmediately:false` path (D-05) and does NOT change the forked just_audio package (D-06), and the full playback_trust_test.dart suite remaining green.
</objective>

<execution_context>
@GSD core/workflows/execute-plan.md
@GSD core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cold-restore-position-stream-bridge-fix/05-CONTEXT.md

# Fix surface (position-stream bridge) + its only consumer
@lib/resources/services/my_audio_handler.dart
@lib/screens/audiobook_player/widgets/progress_bar_widget.dart

# Regression surface (FakePlaybackEngine + the two invariants this plan MUST NOT break)
@test/playback_trust_test.dart
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (RED): Extend FakePlaybackEngine with deferred-load mode and add the failing cold-restore position-stream regression test</name>
  <files>test/playback_trust_test.dart</files>
  <read_first>
    - .planning/phases/05-cold-restore-position-stream-bridge-fix/05-CONTEXT.md (PRIMARY INPUT — especially D-04a/D-04b candidate mechanisms, D-05 no-seek invariant, D-10 why the current fake masks the bug)
    - test/playback_trust_test.dart (FakePlaybackEngine at ~§614-763, the restore test at §211-242, the D-05 no-seek invariant at §244-290, the playImmediately:true ready-drive test at §292-320 showing how processingStates.add(ProcessingState.ready) unblocks the await-ready gate, SetAudioSourcesCall/SeekCall DTOs at §765-784)
    - lib/resources/services/my_audio_handler.dart (getPositionStream at §785-811 showing Rx.combineLatest3 + live _player.duration getter at §800-803; play() override at §848-857; restoreIfNeeded at §279-283; _restoreQueueFromBoxIfEmpty at §813-837; initSongs playImmediately:false deferred-load path at §584-590; PositionData at §1021)
    - .planning/ROADMAP.md (Phase 5 success criteria)
    - .planning/REQUIREMENTS.md (RESTORE-01)
  </read_first>
  <action>
    Extend FakePlaybackEngine (test/playback_trust_test.dart ~§614) so it can mirror the forked just_audio deferred-load semantics described in CONTEXT.md D-10. Add a `bool deferPositionEmission` field defaulting to false, and set it true inside setAudioSources (§718) ONLY when the `preload` argument is false — mirroring the fork's "preload:false defers the native load until play()". When deferPositionEmission is true, setAudioSources must record initialIndex/initialPosition into the fake's state (currentIndex, position, sequence, playbackEvent, currentIndexes.add) but must NOT call positions.add(initialPosition) and must NOT resolve duration to a non-null value yet (leave the engine's `duration` field set to null so the live _player.duration getter in getPositionStream returns null, exercising the D-04a branch). The deferred load is then "resolved" by play() (§684): when deferPositionEmission is true, play() must set `duration` back to the fake's known non-zero value (Duration(minutes:5)), add the stored initialPosition to positions, add bufferedPosition to bufferedPositions, and reset deferPositionEmission to false — mirroring the fork applying initialSeekValues + resolving the duration probe on play(). Guard all of this behind the flag so the existing 16 tests (which use the default synchronous-emission fake) keep passing byte-for-byte. Keep seek() (§692) unchanged — the fake still emits on seek for the existing playImmediately:true / skipToQueueItem / seek-persist tests.

    Then add a NEW failing test in the "MyAudioHandler with fake playback engine" group (after the §211-242 restore test) titled 'cold-restore + play emits restored position and non-zero duration on getPositionStream (deferred-load regression)' with a comment citing D-04a/D-04b and D-10. The test must: (1) seed the Hive box with audiobook + files + index 1 + position 123456 (same shape as the §211-242 restore test); (2) construct the handler with the fake and call handler.restoreIfNeeded() — this triggers _restoreQueueFromBoxIfEmpty → initSongs(playImmediately:false) → fake.setAudioSources(preload:false) which (with the new deferred mode) does NOT emit position and leaves duration null; (3) assert the deferred-load preconditions: fake.setAudioSourcesCalls.single.preload is false and fake.seekCalls is empty (the D-05 invariant must hold before play); (4) call handler.play() — this triggers _player.play() which resolves the deferred load in the fake (emits position, resolves duration); (5) subscribe to handler.getPositionStream() BEFORE step 4 and, using a bounded window (await stream.first.timeout(Duration(seconds:1)) or a small emission loop), assert the emitted PositionData has position equal to Duration(milliseconds:123456) (the saved position, > Duration.zero) AND duration greater than Duration.zero. Run the test and CONFIRM IT FAILS — this reproduces the bug. Capture in the SUMMARY which of D-04a vs D-04b the failure mechanism matches (likely both: position not emitted AND duration null). Do NOT add any seek in this test or anywhere in the playImmediately:false path (D-05). Do NOT modify lib/ in this task.
  </action>
  <verify>
    Run: flutter test test/playback_trust_test.dart

    The new regression test MUST FAIL (RED) with an assertion failure on the PositionData position or duration — confirming the bug is reproduced in the fake. All other 16 existing tests in the same file MUST still PASS (the default-deferPositionEmission=false path is unchanged). A failure of any existing test indicates the FakePlaybackEngine change broke the synchronous-emission contract — revise the flag gating before proceeding.
  </verify>
  <acceptance_criteria>
    - FakePlaybackEngine has a new field (e.g. deferPositionEmission) that, when set true by setAudioSources(preload:false), suppresses the positions.add(initialPosition) call and leaves duration null until play() resolves the deferred load.
    - FakePlaybackEngine.play() resolves the deferred load when the flag is set: sets duration to a non-zero value, emits the stored initialPosition on positions, emits bufferedPosition on bufferedPositions, and resets the flag.
    - A new test 'cold-restore + play emits restored position and non-zero duration on getPositionStream (deferred-load regression)' exists in the fake-engine group and FAILS at the position/duration assertion (RED confirmed).
    - The new test asserts the D-05 preconditions before play (seekCalls is empty, setAudioSourcesCalls.single.preload is false).
    - grep -c 'deferPositionEmission' test/playback_trust_test.dart returns >= 4 (field decl + set in setAudioSources + read/suppress + play resolve).
    - The existing 16 tests still pass: flutter test test/playback_trust_test.dart reports exactly 1 failure (the new RED test) and 0 failures among the pre-existing tests.
  </acceptance_criteria>
</task>

<task type="tdd">
  <name>Task 2 (GREEN): Fix the position-stream bridge so getPositionStream emits the restored position and resolved duration after the deferred load</name>
  <files>lib/resources/services/my_audio_handler.dart</files>
  <read_first>
    - .planning/phases/05-cold-restore-position-stream-bridge-fix/05-CONTEXT.md (D-04a/D-04b confirmed mechanism from Task 1, D-05 no-seek prohibition, D-06 keep the await-ready gate, D-08 fix layer is the position-stream bridge, D-07 no new deps)
    - lib/resources/services/my_audio_handler.dart (getPositionStream at §785-811 — Rx.combineLatest3 over _player.positionStream/bufferedPositionStream/currentIndexStream with a LIVE _player.duration getter at §800-803; play() override at §848-857 which awaits _player.play() then _broadcastState; restoreIfNeeded at §279-283; imports at §1-19 — rxdart already imported at §17, so BehaviorSubject/Rx.startWith/doOnData are available with NO new dependency; PositionData at §1021)
    - test/playback_trust_test.dart (the RED test added in Task 1 — this is the spec the fix must satisfy; the D-05 no-seek invariant at §244-290 which must stay green)
    - lib/screens/audiobook_player/widgets/progress_bar_widget.dart (the single consumer — confirms getPositionStream output shape must stay PositionData)
  </read_first>
  <action>
    Fix the position-stream bridge in lib/resources/services/my_audio_handler.dart so that after the forked deferred native load resolves, getPositionStream() emits a PositionData carrying the restored position (> Duration.zero) and the resolved duration (> Duration.zero). The fix layer is locked to the position-stream bridge (D-08) — do NOT touch the initSongs play sequence (§559-583 await-ready gate stays intact per D-06) and do NOT add any seek() in the playImmediately:false path (D-05).

    Concrete mechanism (rxdart already imported at §17, so no new dependency per D-07): the confirmed root cause is that combineLatest3 (§786) only re-emits when position/buffered/currentIndex re-emit, and on the deferred-load path those streams do not re-emit the restored position until after play() resolves the native load — while the live _player.duration getter (§800) is null until the duration probe returns. Bridge both gaps by re-seeding the position + duration into the combined stream once the deferred load resolves. Implement this by: (a) in the play() override (§848-857), after `await _player.play();` returns (i.e. after the fork has applied initialSeekValues and resolved the duration probe), emit a position refresh onto the engine's positionStream and bufferedPositionStream so combineLatest3 re-fires with the now-restored position and the now-resolved duration — read the live _player.position / _player.bufferedPosition / _player.duration getters at that point. Concretely, expose a method on PlaybackEngine (e.g. `void emitPositionState()` or reuse existing stream sinks) that MyAudioHandler.play() calls after _player.play() resolves; JustAudioPlaybackEngine forwards to the real player's position re-read, and FakePlaybackEngine (test) already has broadcast StreamControllers (positions/bufferedPositions) it can add to. ALTERNATIVELY (Claude's discretion per CONTEXT.md), seed the restored position + a duration fallback into getPositionStream itself using rxdart (Rx.startWith / a BehaviorSubject-backed relay that play() pushes into after the deferred load) — but the chosen mechanism MUST (1) require NO new dependency, (2) NOT call seek() anywhere in the playImmediately:false path, (3) NOT modify the forked just_audio package, (4) NOT break the await-ready gate at §565-577, and (5) keep getPositionStream's return type as Stream<PositionData> so ProgressBarWidget is unchanged. Add a comment citing D-04a/D-04b + D-08 at the fix site.

    If the duration is still null in the immediate post-play emission (probe lag), also widen the duration fallback at §800-803 so that when _player.duration is null/zero but the engine has a known non-zero duration (or the metadata durationMs is non-zero), the emitted PositionData carries a non-zero duration rather than Duration.zero — this directly addresses the D-04a branch and keeps the bar renderable.
  </action>
  <verify>
    Run: flutter test test/playback_trust_test.dart

    The RED test from Task 1 MUST now PASS (GREEN): after restoreIfNeeded() + play(), getPositionStream() emits a PositionData with position == Duration(milliseconds:123456) and duration > Duration.zero within the bounded window. ALL 16 pre-existing tests MUST still pass, including the D-05 no-seek invariant at §244-290 (seekCalls empty in the playImmediately:false path) and the restore test at §211-242. Run the analyzer: flutter analyze lib/resources/services/my_audio_handler.dart — must report no new errors.

    Prohibition probes (hard gates):
    - The forked just_audio package is unchanged: git diff --stat must show zero changes under pub-cache just_audio paths (the package is git-overridden; verify no edits landed). Concretely, the only modified files are test/playback_trust_test.dart and lib/resources/services/my_audio_handler.dart.
    - No seek added in the playImmediately:false path: grep -n 'seek' lib/resources/services/my_audio_handler.dart must show no NEW seek call reachable from the §584-590 deferred-load branch (the existing seek at §562 stays inside the `if (playImmediately)` block).
  </verify>
  <acceptance_criteria>
    - The Task 1 RED test now PASSES: getPositionStream() emits position == saved position and duration > Duration.zero within the bounded window after restoreIfNeeded() + play().
    - flutter test test/playback_trust_test.dart reports 0 failures across the entire file (all pre-existing tests + the new regression test).
    - flutter analyze lib/resources/services/my_audio_handler.dart reports no new errors or warnings vs the pre-fix baseline.
    - The fix introduces NO new dependency (pubspec.yaml / pubspec.lock unchanged for this task; rxdart §17 already imported).
    - git status --short shows changes ONLY in test/playback_trust_test.dart (Task 1) and lib/resources/services/my_audio_handler.dart (this task) — no edits to the forked just_audio package, no edits to ProgressBarWidget.
    - No new seek() call is added in any code path reachable when playImmediately is false (the §562 seek stays inside `if (playImmediately)`).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3 (REGRESSION): Confirm the diagnostic now passes, lock the deferred-load fake mode with a doc comment, and verify the full suite + D-05/D-restore invariants stay green</name>
  <files>test/playback_trust_test.dart</files>
  <read_first>
    - .planning/phases/05-cold-restore-position-stream-bridge-fix/05-CONTEXT.md (D-09 diagnostic-first approach — this task closes the RED→GREEN→invariants loop; D-10 why the fake needed the deferred mode)
    - test/playback_trust_test.dart (the regression test added in Task 1, the FakePlaybackEngine deferred-mode fields added in Task 1, the D-05 no-seek invariant at §244-290, the restore test at §211-242, the gen-discard invariant at §440-490)
    - lib/resources/services/my_audio_handler.dart (the fix landed in Task 2 — confirm the contract the test asserts matches the shipped code)
  </read_first>
  <action>
    Confirm the diagnostic loop is closed. (1) Add or expand the doc comment on the FakePlaybackEngine deferred-load mode (the deferPositionEmission field and its setAudioSources/play handling from Task 1) so a future reader understands it mirrors the forked just_audio preload:false semantics — reference CONTEXT.md D-10 and the fork ref a6f8db8, and state that the regression test exercises the gap the synchronous-emission fake used to mask. (2) Run the FULL playback_trust_test.dart suite and confirm every test passes, paying specific attention to: the new regression test (Task 1, now GREEN), the D-05 no-seek invariant at §244-290 (seekCalls empty in the playImmediately:false path), the restore test at §211-242 (queue restored without real playback, playCount 0), the playImmediately:true ready-driven test at §292-320 (seek still issued after synchronous load), the skipToQueueItem test at §322-343, and the gen-discard/stale-init invariant at §440-490. (3) If any pre-existing test regressed, that is a Rule-1 bug in the Task 2 fix — fix it inline and re-run; do NOT weaken an existing assertion to make it pass. (4) Do NOT expand scope to RESTORE-02 (streaming duration probe) or RESTORE-03 (mini-player sync) — those are deferred per REQUIREMENTS.md.
  </action>
  <verify>
    Run the complete suite: flutter test test/playback_trust_test.dart

    Expected: ALL tests pass (the 16 pre-existing tests + the new regression test from Task 1). The suite must report 0 failures. Specifically assert by inspection: the D-05 no-seek test passes (seekCalls empty), the restore test passes (playCount 0, queue titles correct), the playImmediately:true test passes (seekCalls non-empty with the saved position), and the new regression test passes (position + duration non-zero within the window).
  </verify>
  <acceptance_criteria>
    - flutter test test/playback_trust_test.dart reports 0 failures for the entire file.
    - The FakePlaybackEngine deferred-load mode has a doc/comment block explaining it mirrors the forked just_audio preload:false deferred-load semantics, citing CONTEXT.md D-10 and fork ref a6f8db8.
    - The D-05 no-seek invariant test (§244-290) passes unchanged — its assertion text and seekCalls-emptiness check are byte-for-byte identical to the pre-fix baseline.
    - The restore test (§211-242) passes unchanged — playCount is 0 and the queue is restored from Hive.
    - No assertion in any pre-existing test was weakened or deleted to make the suite pass (diff shows only additions: the new field, the new deferred-mode handling, the doc comment, and the new test).
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. flutter test test/playback_trust_test.dart — entire suite green (16 pre-existing + 1 new regression test). This is the primary automated gate.

2. flutter analyze lib/resources/services/my_audio_handler.dart — no new errors/warnings vs baseline.

3. Prohibition gate — forked just_audio package unchanged:
   git status --short must list ONLY test/playback_trust_test.dart and lib/resources/services/my_audio_handler.dart. No path under the just_audio git override is modified.

4. Prohibition gate — no seek in the playImmediately:false path:
   The only seek() call in lib/resources/services/my_audio_handler.dart reachable from initSongs remains the existing one inside the `if (playImmediately)` block at §562. The §584-590 deferred-load branch and the play() override introduce no new seek().

5. Invariant gate — D-05 no-seek test (§244-290) and restore test (§211-242) pass with assertion text unchanged.
</verification>

<success_criteria>
- RESTORE-01 (position + duration shown within first second of playback after cold-restore) is delivered at the unit-test level: the regression test proves getPositionStream() emits the saved position and a non-zero duration after restoreIfNeeded() + play() within a bounded window.
- The full playback_trust_test.dart suite is green, preserving the v1.0 invariants (D-05 no-seek, restore-without-playback, gen-discard).
- The fix is confined to the position-stream bridge layer (D-08): no change to the initSongs await-ready gate (D-06), no change to the forked just_audio package, no new dependency (D-07).
- Roadmap success criteria 1, 2, and 4 are covered by automated tests; criterion 3 (seek still works — no jump-back, no stuck thumb) is structurally guaranteed because the fix only ADDS a position re-emission and never removes or races the existing seek path (on-device manual confirmation of criteria 1-3 is captured separately as the v1.1 UAT smoke, mirroring v1.0's 04-UAT.md pattern).
</success_criteria>

<output>
Create `.planning/phases/05-cold-restore-position-stream-bridge-fix/05-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

### Modified symbols (lib/resources/services/my_audio_handler.dart)
- `getPositionStream()` (~§785-811) — the position-stream bridge is widened so it emits the restored position + resolved (or fallback) duration after the deferred native load; the live `_player.duration` fallback at §800-803 is strengthened to yield a non-zero duration when the probe lags.
- `play()` override (~§848-857) — after `await _player.play();` resolves the deferred load, the bridge re-seeds position/buffered/duration so `Rx.combineLatest3` re-fires; NO new seek() is introduced.
- (Claude's discretion per CONTEXT.md) A new private helper or PlaybackEngine method (e.g. `emitPositionState()` on the abstract `PlaybackEngine` at §40 + `JustAudioPlaybackEngine` at §79 + `FakePlaybackEngine` in test) that re-emits current position state, called from `play()` after the deferred load resolves. Exact name chosen during implementation; must not add a dependency or break the abstract surface.

### Modified symbols (test/playback_trust_test.dart)
- `FakePlaybackEngine` (~§614-763) — gains a deferred-load mode (field + `setAudioSources` suppression when `preload:false` + `play()` resolution) mirroring the forked just_audio semantics, with a doc comment citing CONTEXT.md D-10 and fork ref a6f8db8.
- New regression test `'cold-restore + play emits restored position and non-zero duration on getPositionStream (deferred-load regression)'` in the fake-engine group — the RED→GREEN spec for RESTORE-01.

### Unchanged (prohibited)
- Forked just_audio package (git override `sagarchaulagai/just_audio.git @ a6f8db8`) — zero edits.
- `lib/screens/audiobook_player/widgets/progress_bar_widget.dart` — the only consumer; getPositionStream() return type stays `Stream<PositionData>`.
- `initSongs` await-ready gate (§565-577) and the `playImmediately:false` no-seek branch (§584-590) — byte-for-byte preserved (D-05, D-06).
- `pubspec.yaml` / `pubspec.lock` — no new dependency (D-07).
