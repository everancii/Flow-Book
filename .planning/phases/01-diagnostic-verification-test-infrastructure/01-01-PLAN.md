---
phase: 01-diagnostic-verification-test-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/playback_trust_test.dart
  - lib/resources/services/my_audio_handler.dart
autonomous: true
requirements:
  - TEST-01

must_haves:
  truths:
    - "A developer running `flutter test` sees new test cases that configure FakePlaybackEngine to start in ProcessingState.loading and emit ProcessingState.ready on its processingStates stream — proving the loading→ready transition is simulated."
    - "A developer running `flutter test` sees a skipped test whose assertion (playCount == 0 before ready) is the race detector that fails today and passes after the Phase 3 fix — written but skipped so the suite stays green until the fix lands."
    - "A developer opening any book on a real device sees [DIAG]-tagged lines in the app log file showing the processingState path through initSongs, whether setAudioSources threw, and the playing flag 500ms after play()."
    - "The existing 520-line playback_trust_test.dart suite stays green — no existing test modified, no existing assertion changed."
    - "No production behavior changes — diagnostic try/catch around setAudioSources rethrows (preserves propagation to _autoPlay catch at audiobook_details.dart:133)."
  artifacts:
    - "test/playback_trust_test.dart — 2 new test cases inside the existing 'MyAudioHandler with fake playback engine' group (line 210), after the last existing test (line 291)"
    - "lib/resources/services/my_audio_handler.dart — 4 new AppLogger.debug calls tagged [DIAG] in initSongs (checkpoints 1, 2, 4, 5); checkpoint 3 reuses the existing log at line 563"
    - "lib/resources/services/my_audio_handler.dart — try/catch wrapper around the setAudioSources call (lines 540-547) that logs then rethrows"
  key_links:
    - "FakePlaybackEngine.processingState field (line 384) ↔ test configuration sets it to ProcessingState.loading before initSongs"
    - "FakePlaybackEngine.processingStates StreamController (line 357) ↔ test emits ProcessingState.ready via processingStates.add() after a 10ms event-loop pump"
    - "FakePlaybackEngine.processingStateStream getter (line 411) ↔ returns processingStates.stream — the surface the Phase 3 fix will listen on"
    - "[DIAG] log strings ↔ AppLogger.debug writes to external storage log file unconditionally (app_logger.dart:95) — visible on release-device builds"
    - "Diagnostic try/catch rethrow ↔ _autoPlay catch block (audiobook_details.dart:133) receives exceptions unchanged"
---

## Phase Goal

**As a** developer fixing the Sound-Books auto-play bug, **I want to** reproduce the loading→ready ProcessingState race in the test suite and confirm the actual failure mechanism on a real device with diagnostic logs, **so that** the Phase 3 fix targets the right layer and is verifiable before it is written.

<objective>
Extend the existing FakePlaybackEngine test infrastructure so the loading→ready race is reproducible in `flutter test`, and add temporary [DIAG]-tagged diagnostic logs to `MyAudioHandler.initSongs` so the failure mechanism can be confirmed on a real device. This plan does NOT fix the bug and does NOT change production behavior.

Purpose: Phase 3's fix is untestable without a fake that can simulate the loading→ready transition (TEST-01), and the "play() dropped during buffering" hypothesis is unconfirmed without on-device diagnostic data. This plan delivers both precursors.
Output: 2 new test cases in playback_trust_test.dart + 4 new [DIAG] log checkpoints in initSongs. No new files, no new classes, no new dependencies.
</objective>

<execution_context>
@/Users/onlyeverancii/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/onlyeverancii/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-diagnostic-verification-test-infrastructure/01-RESEARCH.md
@.planning/phases/01-diagnostic-verification-test-infrastructure/01-PATTERNS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@lib/resources/services/my_audio_handler.dart
@test/playback_trust_test.dart
@lib/utils/app_logger.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add loading→ready simulation tests to playback_trust_test.dart</name>
  <files>test/playback_trust_test.dart</files>
  <read_first>
    - test/playback_trust_test.dart (FULL read — current 520-line file: imports lines 1-15, Hive setUp lines 20-45, test group 'MyAudioHandler with fake playback engine' lines 210-292, sample helpers _sampleAudiobook/_sampleFiles lines 295-336, FakePlaybackEngine class lines 350-499)
    - lib/resources/services/my_audio_handler.dart (initSongs signature lines 416-422, play() call site line 565, _waitForProcessingReady lines 650-657 — to confirm what the test exercises)
    - .planning/phases/01-diagnostic-verification-test-infrastructure/01-RESEARCH.md (Pattern 1 loading→ready config, Pitfall 1 BehaviorSubject caveat, Pitfall 4 test timing, Code Examples 1-3)
    - .planning/phases/01-diagnostic-verification-test-infrastructure/01-PATTERNS.md (test construction pattern lines 66-107, FakePlaybackEngine fields lines 98-105, insertion point line 107)
  </read_first>
  <behavior>
    - Test "fake accepts loading config, play fires unconditionally regardless of processingState": set fake.processingState = ProcessingState.loading, call handler.initSongs(_sampleFiles(), _sampleAudiobook(), 0, 0, playImmediately: true) and await the returned Future (completes fast — sample files are non-YouTube + positionInMilliseconds is 0, so initSongs takes the else branch at line 557 and skips _waitForProcessingReady at 551-554; setAudioSources + seek + play() run synchronously on the fake), assert fake.playCount == 1 (proves play() fires unconditionally even though processingState is stuck at loading — the current bug) and fake.setAudioSourcesCalls hasLength 1. PASSES today. No ProcessingState.ready emission on the stream — the existing processingStateStream listener at my_audio_handler.dart:569-575 re-fires play() on ready, which would double playCount to 2; omitting the emission keeps playCount == 1. Will FAIL after Phase 3 fix (play deferred until ready → playCount stays 0 when ready never arrives) — Phase 3 updates this test.
    - Test "play() does not fire before ready (race condition)": same setup, pump 10ms, assert fake.playCount == 0 BEFORE emitting ready. FAILS today (play() fires at line 565 unconditionally), PASSES after Phase 3 fix. Marked @Skip('await Phase 3 ready-before-play fix') so suite stays green.
  </behavior>
  <action>
    Add exactly 2 new test cases INSIDE the existing group('MyAudioHandler with fake playback engine', ...) block (line 210), inserted after the last existing test (the 'seek writes latest position...' test ending at line 291, before the group's closing brace at line 292). Do NOT modify FakePlaybackEngine's class definition (lines 350-499) — the existing mutable processingState field (line 384, default ProcessingState.ready) and broadcast processingStates StreamController (line 357) are sufficient. Do NOT add any rxdart import (broadcast StreamController is sufficient for field-check-first pattern; BehaviorSubject upgrade is Phase 3 scope if needed).

    Both tests reuse the existing _sampleFiles() and _sampleAudiobook() helpers (lines 295-336) — do not redefine them. Both construct the handler with the verbatim pattern: final fake = FakePlaybackEngine(); final handler = MyAudioHandler(player: fake, configureAudioSession: false); — configureAudioSession: false is mandatory to avoid real audio-session activation.

    TEST 1 (passing today — proves the fake accepts loading config + play fires unconditionally): name it 'initSongs fires play() unconditionally even when processingState stays loading'. Set fake.processingState = ProcessingState.loading before calling initSongs. Call handler.initSongs(_sampleFiles(), _sampleAudiobook(), 0, 0, playImmediately: true) and await the returned Future directly (no intermediate pump needed — the sample files are non-YouTube and positionInMilliseconds is 0, so initSongs takes the else branch at line 557 and never calls _waitForProcessingReady at 551-554; setAudioSources, seek, and play() all run synchronously on the fake). Assert expect(fake.playCount, 1) and expect(fake.setAudioSourcesCalls, hasLength(1)). Do NOT emit ProcessingState.ready on fake.processingStates — the existing processingStateStream listener at my_audio_handler.dart:569-575 calls _player.play() again when state == ProcessingState.ready, which would make playCount == 2; keeping processingState at loading and never emitting on the stream means the listener never re-fires, so playCount stays at 1 (the single unconditional play() at line 565). This test proves (a) the fake accepts a loading configuration — the infrastructure proof that loading→ready simulation is configurable — and (b) play() fires unconditionally regardless of processingState (the current bug Phase 3 will fix). It passes today and WILL FAIL after the Phase 3 fix (play deferred until ready → playCount stays 0 when ready never arrives); Phase 3 must update this test to emit ready on the stream once the listener re-fire is removed.

    TEST 2 (the race detector — fails today, passes after Phase 3 fix): name it 'play() does not fire before processingState reaches ready (race detector)'. Annotate with @Skip('await Phase 3 ready-before-play fix') so flutter test skips it and the suite stays green. Same construction + fake.processingState = ProcessingState.loading setup. Call handler.initSongs(..., playImmediately: true) capturing initFuture. Pump 10ms. Assert expect(fake.playCount, 0, reason: 'play() must not fire before processingState reaches ready') — this is the assertion that fails today (playCount is 1 because line 565 fires unconditionally) and passes after the Phase 3 fix (play deferred until ready). Then emit ready (fake.processingState = ProcessingState.ready; fake.processingStates.add(ProcessingState.ready)), await initFuture, assert expect(fake.playCount, 1). Include a comment on the skip annotation: '// Remove @Skip after Phase 3 implements ready-before-play. This test verifies the race is gone.'

    Do NOT touch any existing test (lines 210-291). Do NOT change setUp/setUpAll/tearDown. Do NOT add new imports — ProcessingState already comes from package:just_audio/just_audio.dart (line 15).
  </action>
  <verify>
    <automated>cd /Users/onlyeverancii/Desktop/Project/FlowBook && flutter test test/playback_trust_test.dart 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/playback_trust_test.dart` exits 0 — full suite green including the 2 new tests (test 1 passes, test 2 skipped).
    - `rg -c 'initSongs fires play\(\) unconditionally even when processingState stays loading' test/playback_trust_test.dart` returns 1.
    - `rg -c "play\(\) does not fire before processingState reaches ready" test/playback_trust_test.dart` returns 1.
    - `rg -c "@Skip\('await Phase 3 ready-before-play fix'\)" test/playback_trust_test.dart` returns 1.
    - FakePlaybackEngine class body (lines 350-499) is byte-identical to pre-phase — `rg -c 'ProcessingState processingState = ProcessingState.ready' test/playback_trust_test.dart` returns 1 (field default unchanged).
    - No rxdart import added — `rg -c 'rxdart' test/playback_trust_test.dart` returns 0.
    - New tests are inside the existing group — `rg -n 'group\(.MyAudioHandler with fake playback engine' test/playback_trust_test.dart` shows the group at line 210 and new tests appear after line 291.
  </acceptance_criteria>
  <done>
    Two new test cases exist inside the existing fake-playback-engine group. Test 1 passes today (proves the fake accepts loading config + play fires unconditionally — the bug Phase 3 fixes; will need updating when the fix lands). Test 2 is skipped with a clear annotation (the race detector that fails today, passes after Phase 3). The full 520-line suite plus the 2 new tests pass/skip cleanly under `flutter test`. FakePlaybackEngine class is unmodified. No new imports.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add [DIAG]-tagged diagnostic checkpoints to initSongs</name>
  <files>lib/resources/services/my_audio_handler.dart</files>
  <read_first>
    - lib/resources/services/my_audio_handler.dart (initSongs lines 416-642: myGen at 424, gen-guard returns at 526/549/622, setAudioSources call at 540-547 NO try/catch currently, play() call at 565 with existing log at 563-564, existing listener block 567-605, 60s cancel at 608, orphan listener 611-613, finally block 639-641)
    - lib/utils/app_logger.dart (debug signature lines 90-96: static void debug(String message, [String? tag]); file write at line 95 is unconditional; kDebugMode only gates print at 92-94)
    - .planning/phases/01-diagnostic-verification-test-infrastructure/01-RESEARCH.md (Pattern 2 diagnostic logging protocol, Pitfall 2 rethrow-not-swallow, Pitfall 3 gen-check on delayed log, Anti-Patterns: no kDebugMode gate, don't remove existing logs)
    - .planning/phases/01-diagnostic-verification-test-infrastructure/01-PATTERNS.md (existing logging pattern lines 121-152, setAudioSources call site 165-176, play() call site 178-184, exact diagnostic log strings 186-224, AppLogger.debug contract 226-236)
    - test/playback_trust_test.dart (FakePlaybackEngine.setAudioSources lines 454-474 — does NOT change processingState; play() lines 421-425 sets playing=true — confirms what the diagnostic logs will observe)
  </read_first>
  <action>
    Add 4 new AppLogger.debug calls tagged with the [DIAG] prefix inside the initSongs method. Checkpoint 3 (before play) already exists at line 563 — do NOT duplicate it, do NOT remove it. Use the exact log format from PATTERNS.md lines 186-224. All logs interpolate the local variable myGen (declared at line 424) and the active flag (myGen == _initGen) so stale-init logs are clearly marked. Use AppLogger.debug with NO tag argument (matches existing initSongs logs which use the default 'Flow Book' tag).

    CHECKPOINT 1 — insert immediately ABOVE the setAudioSources call (currently line 540), after the `final currentIsYT = _isIndexYouTube(safeIndex);` line (538). One AppLogger.debug call: string starts with '[DIAG] initSongs[gen=$myGen,active=${myGen == _initGen}]: before setAudioSources, ' followed by 'processingState=${_player.processingState}, playing=${_player.playing}'. Use adjacent string literal concatenation (matches line 563-564 style).

    CHECKPOINT 2 — wrap the EXISTING setAudioSources call (lines 540-547) in a try/catch. The try block keeps the existing await _player.setAudioSources(...) call verbatim (same arguments: _audioSources!, initialIndex: sources.isEmpty ? 0 : safeIndex, initialPosition: currentIsYT ? Duration.zero : Duration(milliseconds: positionInMilliseconds), preload: playImmediately). After the setAudioSources call inside the try block, add one AppLogger.debug: '[DIAG] initSongs[gen=$myGen,active=${myGen == _initGen}]: setAudioSources OK, processingState=${_player.processingState}'. The catch block catches (e), adds one AppLogger.debug: '[DIAG] initSongs[gen=$myGen,active=${myGen == _initGen}]: setAudioSources THREW: $e, processingState=${_player.processingState}', then rethrows. The rethrow is mandatory — preserves existing exception propagation to _autoPlay's catch block (audiobook_details.dart:133) and _playChapter's catch (audiobook_details.dart:84). Do NOT swallow the exception.

    CHECKPOINT 4 — insert immediately BELOW the _player.play() call (currently line 565), inside the `if (playImmediately)` block. One AppLogger.debug: '[DIAG] initSongs[gen=$myGen,active=${myGen == _initGen}]: after play(), processingState=${_player.processingState}, playing=${_player.playing}'.

    CHECKPOINT 5 — insert inside the `if (playImmediately)` block, after checkpoint 4, as a fire-and-forget Future.delayed. Use Future.delayed(const Duration(milliseconds: 500), () { ... }). Inside the callback: first line is the gen-staleness guard `if (myGen != _initGen) return;` (matches existing guard pattern at lines 526, 549, 622). Then one AppLogger.debug: '[DIAG] initSongs[gen=$myGen,active=${myGen == _initGen}]: 500ms after play(), processingState=${_player.processingState}, playing=${_player.playing}' plus a conditional suffix: if _player.playing is false, append ' <- audioSession.setActive may have failed' (this detects the fork's play-reversion mechanism at fork lines 1106-1127). Use a ternary in the string: '${_player.playing ? '' : ' <- audioSession.setActive may have failed'}'.

    Do NOT remove or modify the existing logs at lines 563-564, 570, 573, 581, 593, 612 — those are part of the current play-init flow and are cleaned up in Phase 2/3. Do NOT add a kDebugMode guard around any [DIAG] log — AppLogger.debug already gates print on kDebugMode (app_logger.dart:92) but writes to the file unconditionally (line 95); the diagnostic logs MUST appear in the file log on a release device, which is the whole point. Do NOT change the setAudioSources arguments, the play() call, the listener block (567-605), the 60s cancel (608), or the orphan listener (611-613) — Phase 1 only ADDS logs. Do NOT add any new import — AppLogger is already imported at line 12.
  </action>
  <verify>
    <automated>cd /Users/onlyeverancii/Desktop/Project/FlowBook && flutter test test/playback_trust_test.dart 2>&1 | tail -20 && flutter analyze lib/resources/services/my_audio_handler.dart 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/playback_trust_test.dart` exits 0 — existing 520-line suite + Task 1's new tests still green (diagnostic logs don't change behavior).
    - `flutter analyze lib/resources/services/my_audio_handler.dart` reports no new issues.
    - `rg -c '\[DIAG\]' lib/resources/services/my_audio_handler.dart` returns 5 (checkpoints 1, 2-success, 2-threw, 4, 5 = 5 log statements; checkpoint 2 has two [DIAG] strings in try and catch — count the [DIAG] token occurrences: checkpoint 1 = 1, checkpoint 2 try = 1, checkpoint 2 catch = 1, checkpoint 4 = 1, checkpoint 5 = 1 → total 5 [DIAG] occurrences).
    - `rg -c 'rethrow' lib/resources/services/my_audio_handler.dart` returns at least 1 (the diagnostic catch block rethrows).
    - `rg -c 'if \(myGen != _initGen\) return;' lib/resources/services/my_audio_handler.dart` returns at least 4 (existing 3 at lines 526/549/622 + new 1 in checkpoint 5 delayed callback).
    - `rg -c 'audioSession.setActive may have failed' lib/resources/services/my_audio_handler.dart` returns 1 (checkpoint 5 suffix).
    - The existing log at line 563 ('initSongs: calling _player.play(), state=') is still present — `rg -c 'initSongs: calling _player.play' lib/resources/services/my_audio_handler.dart` returns 1.
    - `rg -c 'Future.delayed\(const Duration\(milliseconds: 500\)' lib/resources/services/my_audio_handler.dart` returns at least 2 (existing idle-recovery at line 582 + new checkpoint 5).
  </acceptance_criteria>
  <done>
    5 new [DIAG]-tagged AppLogger.debug calls exist in initSongs (checkpoints 1, 2-try, 2-catch, 4, 5). The setAudioSources call is wrapped in try/catch that logs and rethrows. Checkpoint 5's 500ms delayed callback has a gen-staleness guard. No existing logs removed. No kDebugMode guard added. flutter test green, flutter analyze clean. No behavior change — the diagnostic logs are observation-only.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer→log-file | [DIAG] log strings are developer-authored interpolation of internal state (ProcessingState enum, playing bool, myGen int, exception toString). No user input flows into log strings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | AppLogger file output (log/applogs.txt on external storage) | low | accept | [DIAG] logs add ProcessingState enum values + playing boolean + exception toString — no credentials, no PII, no user input. Existing CONCERNS.md flag about external-storage logging applies but is not worsened. Risk unchanged from pre-phase. |
| T-01-02 | Tampering | Log injection via interpolated exception toString | low | mitigate | catch (e) interpolates $e into log — exception messages from setAudioSources are library-internal (just_audio/HTTP), not user-controlled input. No new input path introduced. No mitigation beyond existing AppLogger behavior needed. |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase — all dependencies already in pubspec.yaml. No supply-chain surface added. |
</threat_model>

<verification>
1. `flutter test test/playback_trust_test.dart` — full suite green (existing 520 lines + 2 new tests: 1 passes, 1 skipped).
2. `flutter analyze lib/resources/services/my_audio_handler.dart` — no new issues.
3. `rg '\[DIAG\]' lib/resources/services/my_audio_handler.dart` — 5 occurrences (checkpoints 1, 2-try, 2-catch, 4, 5).
4. `rg 'rethrow' lib/resources/services/my_audio_handler.dart` — diagnostic catch block rethrows (behavior preserved).
5. `rg '@Skip' test/playback_trust_test.dart` — race-detector test skipped so suite stays green until Phase 3.
6. FakePlaybackEngine class unmodified — `rg 'class FakePlaybackEngine' test/playback_trust_test.dart` shows the class with no new methods or fields.
</verification>

<success_criteria>
- TEST-01 delivered: FakePlaybackEngine simulates loading→ready transition via test-code configuration (no class changes needed — existing mutable processingState field + broadcast processingStates stream sufficient).
- A "fails today, passes after fix" race-detector test is written (skipped until Phase 3) — the assertion `expect(fake.playCount, 0)` before ready emission is the race proof.
- 5 [DIAG] diagnostic checkpoints added to initSongs — on-device logs will show the exact processingState path, whether setAudioSources threw, and whether playing reverted to false 500ms after play().
- No production behavior change — try/catch rethrows, no existing logs removed, no kDebugMode guard, no new dependencies.
- Existing 520-line test suite stays green.
</success_criteria>

<output>
Create `.planning/phases/01-diagnostic-verification-test-infrastructure/01-01-SUMMARY.md` when done.
</output>

## Artifacts This Phase Produces

| Artifact | Type | Location | Purpose |
|----------|------|----------|---------|
| Test: 'initSongs fires play() unconditionally even when processingState stays loading' | test case | test/playback_trust_test.dart (inside 'MyAudioHandler with fake playback engine' group) | Proves fake accepts loading config + play fires unconditionally today (fails after Phase 3 — Phase 3 updates it) |
| Test: 'play() does not fire before processingState reaches ready (race detector)' | skipped test case | test/playback_trust_test.dart (same group) | Race detector — @Skip until Phase 3; assertion `playCount == 0` before ready fails today, passes after fix |
| [DIAG] checkpoint 1 log | AppLogger.debug call | my_audio_handler.dart initSongs (before setAudioSources, ~line 540) | Logs processingState + playing before the duration probe |
| [DIAG] checkpoint 2 log (try + catch) | AppLogger.debug call + try/catch wrapper | my_audio_handler.dart initSongs (wrapping setAudioSources lines 540-547) | Logs whether setAudioSources succeeded or threw; rethrow preserves propagation |
| [DIAG] checkpoint 4 log | AppLogger.debug call | my_audio_handler.dart initSongs (after _player.play(), ~line 565) | Logs processingState + playing immediately after play() |
| [DIAG] checkpoint 5 log | AppLogger.debug call in Future.delayed(500ms) | my_audio_handler.dart initSongs (inside if(playImmediately) block) | Logs playing 500ms after play() — detects audioSession.setActive reversion; gen-guarded |
</output>
</invoke>